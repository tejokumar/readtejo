'use strict';
/*
angular.module('books').run(['Menus', 'Authentication', function (Menus, Authentication) {
  Menus.addMenuItem('topbar', {
    title: 'Books',
    state: 'books',
    type: 'dropdown',
    roles: ['*']
  });

    // Add the dropdown list item
  Menus.addSubMenuItem('topbar', 'books', {
    title: 'List Books',
    state: 'books.list'
  });

  // Add the dropdown create item
  Menus.addSubMenuItem('topbar', 'books', {
    title: 'Create Book',
    state: 'books.create'
  });

  Menus.addSubMenuItem('topbar', 'books', {
    title: 'Play Sample',
    state: 'books.view'
  });
}]);
*/